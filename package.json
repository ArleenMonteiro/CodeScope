{
  "name": "web-app",
  "private": true,
  "workspaces": [
    "apps/revuc/revuc",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^1.10.13"
  },
  "packageManager": "npm@9.4.0",
  "dependencies": {
    "check-node-version": "^4.2.1"
  }
}
